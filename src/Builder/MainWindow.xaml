<Window x:Class="Builder.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Builder"
        xmlns:model="clr-namespace:Model;assembly=Model"
        mc:Ignorable="d"
        Title="Builder" Height="350" Width="1024">
    <DockPanel>
        <Border BorderThickness="0"
                Padding="5"
                DockPanel.Dock="Top">
            <DockPanel>
                <Button x:Name="Browse"
                        DockPanel.Dock="Right"
                        Content="Browse"
                        VerticalAlignment="Center"
                        Padding="10,2,10,2"
                        Click="OnClickBrowseSolution"/>
                <TextBox x:Name="SolutionFilePath"
                         TextWrapping="Wrap"
                         Margin="0,0,10,0"
                         Padding="2"
                         Text="{Binding Solution.Path, Mode=OneWay}" />
            </DockPanel>
        </Border>
        <StackPanel DockPanel.Dock="Top"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    Margin="0,0,0,5">
            <StackPanel Orientation="Horizontal"
                        Margin="0,0,30,0">
                <Label>Configuration | Platform</Label>
                <ComboBox ItemsSource="{Binding Solution.ValidConfigurationPlatforms}"
                          Width="100"
                          VerticalAlignment="Center"
                          SelectedItem="{Binding SelectedConfigurationPlatform}"></ComboBox>
            </StackPanel>
            <StackPanel Orientation="Horizontal"
                        Margin="0,0,30,0">
                <Label>Target</Label>
                <ComboBox Width="80"
                          VerticalAlignment="Center"
                          SelectedValue="{Binding BuildTarget}"
                          SelectedValuePath="Name">
                    <ComboBoxItem Name="Build">Build</ComboBoxItem>
                    <ComboBoxItem Name="Rebuild">Rebuild</ComboBoxItem>
                    <ComboBoxItem Name="Clean">Clean</ComboBoxItem>
                </ComboBox>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <Button Content="Build solution, save events"
                        HorizontalAlignment="Center"
                        Padding="10,3"
                        Click="OnClickBuildSolutionSaveEvents"
                        Margin="0,0,5,0"/>
                <Button Content="Build solution, save timeline"
                        HorizontalAlignment="Center"
                        Padding="10,3"
                        Click="OnClickBuildSolutionSaveTimeline"/>
            </StackPanel>
        </StackPanel>
        <ListView Name="BuildMessageList"
                  ItemsSource="{Binding BuildMessages}">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Type}" Value="BuildStartedEventArgs">
                            <Setter Property="Foreground" Value="Green" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Type}" Value="BuildFinishedEventArgs">
                            <Setter Property="Foreground" Value="Green" />
                        </DataTrigger>

                        <DataTrigger Binding="{Binding Type}" Value="ProjectStartedEventArgs">
                            <Setter Property="Foreground" Value="Blue" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Type}" Value="ProjectFinishedEventArgs">
                            <Setter Property="Foreground" Value="Blue" />
                        </DataTrigger>

                        <DataTrigger Binding="{Binding Type}" Value="TaskStartedEventArgs">
                            <Setter Property="Foreground" Value="Gray" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Type}" Value="TaskFinishedEventArgs">
                            <Setter Property="Foreground" Value="Gray" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Timestamp"
                                    Width="Auto">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Timestamp}" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Type"
                                    Width="Auto">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Type}" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Message"
                                    Width="800">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Message}"
                                           TextWrapping="Wrap" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
    </DockPanel>
</Window>
